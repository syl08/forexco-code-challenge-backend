// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // url      = env("DATABASE_URL")
  url      = "postgres://zufuvqdxwrwqap:5209db0cbf3030385ff03c05abf33f5bd07adf6067f4cd71f0ad34ab37ac2b6d@ec2-54-227-248-71.compute-1.amazonaws.com:5432/dci104msh739rh"
}

model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String
  createdAt DateTime @default(now()) @map("created_at")

  @@map("users")
}

model Rate {
  currency       String
  cryptocurrency String
  rate           Float

  @@unique(fields: [currency, cryptocurrency], name: "rateId")
  @@map("rates")
}
